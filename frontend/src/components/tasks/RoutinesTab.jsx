import { View } from "react-native";

import AppText from "../ui/AppText";

const RoutinesTab = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <AppText type="h1">Routines</AppText>
    </View>
  );
};

export default RoutinesTab;
